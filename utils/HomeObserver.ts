// "use client";

// const observer = new IntersectionObserver((entries) => {
//   entries.forEach((entry) => {
//     if (entry.isIntersecting) {
//       // do something
//       console.log("home heading is intersecting");
//       entry.target.classList.add("home-heading-animation");
//     }
//   });
// });

// observer.observe(document.querySelector("#home-heading")!);
